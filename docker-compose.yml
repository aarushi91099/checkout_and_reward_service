services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: checkout_reward
      POSTGRES_USER: checkout
      POSTGRES_PASSWORD: checkout
    ports:
      - "5432:5432"
    volumes:
      - checkout_reward_data:/var/lib/postgresql/data

volumes:
  checkout_reward_data:
